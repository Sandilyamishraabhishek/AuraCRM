# Database Connection String (Defaults to local SQLite db)
# DATABASE_URL=postgresql://user:password@localhost/crm_db
# DATABASE_URL=mysql+pymysql://user:password@localhost/crm_db

# Groq API Key (If provided, enables real LangGraph & LLM execution. 
# If left blank, uses a robust rule-based simulation for entity extraction)
GROQ_API_KEY=
